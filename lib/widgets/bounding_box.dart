import 'package:flutter/material.dart';

class BoundingBox extends StatelessWidget {
  const BoundingBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
